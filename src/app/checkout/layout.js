import React from 'react';
export const metadata = {
    title: "Insocour | Checkout Page",
    description: "Insocour | Checkout Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;