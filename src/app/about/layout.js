import React from 'react';
export const metadata = {
    title: "Insocour | About Page",
    description: "Insocour | About Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;
