import React from 'react';
export const metadata = {
    title: "Insocour | Contact Page",
    description: "Insocour | Contact Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;