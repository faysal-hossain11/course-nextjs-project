import React from 'react';
export const metadata = {
    title: "Insocour | Shop Sidebar Page",
    description: "Insocour | Shop Sidebar Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;