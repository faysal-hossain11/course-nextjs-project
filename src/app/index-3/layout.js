import React from 'react';
export const metadata = {
    title: "Insocour | Home Page Three",
    description: "Insocour | Home Page Three Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;