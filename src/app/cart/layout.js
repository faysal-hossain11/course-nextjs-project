import React from 'react';
export const metadata = {
    title: "Insocour | Cart Page",
    description: "Insocour | Cart Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;