import React from 'react';
export const metadata = {
    title: "Insocour | Courses Page",
    description: "Insocour | Courses Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;