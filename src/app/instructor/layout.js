import React from 'react';
export const metadata = {
    title: "Insocour | Instructor Page",
    description: "Insocour | Instructor Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;