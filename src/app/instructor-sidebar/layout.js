import React from 'react';
export const metadata = {
    title: "Insocour | Instructor Sidebar Page",
    description: "Insocour | Instructor Sidebar Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;