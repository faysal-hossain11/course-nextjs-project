import React from 'react';
export const metadata = {
    title: "Insocour | Course-details Page",
    description: "Insocour | Course-details Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;