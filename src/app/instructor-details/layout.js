import React from 'react';
export const metadata = {
    title: "Insocour | Instructor Details Page",
    description: "Insocour | Instructor Details Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;