import React from 'react';
export const metadata = {
    title: "Insocour | FAQ Page",
    description: "Insocour | FAQ Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;