import React from 'react';
export const metadata = {
    title: "Insocour | Course-list Page",
    description: "Insocour | Course-list Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;