import React from 'react';
export const metadata = {
    title: "Insocour | Tag Page",
    description: "Insocour | Tag Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;