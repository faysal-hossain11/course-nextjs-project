import React from 'react';
export const metadata = {
    title: "Insocour | Sign Up Page",
    description: "Insocour | Sign Up Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;