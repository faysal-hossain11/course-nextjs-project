import React from 'react';
export const metadata = {
    title: "Insocour | Instrurtor Page Two",
    description: "Insocour | Instrurtor Page Two Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;