import React from 'react';
export const metadata = {
    title: "Insocour | Author Page",
    description: "Insocour | Author Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;