import React from 'react';
export const metadata = {
    title: "Insocour | Categories Page",
    description: "Insocour | Categories Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;