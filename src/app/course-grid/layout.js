import React from 'react';
export const metadata = {
    title: "Insocour | Course-grid Page",
    description: "Insocour | Course-grid Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;