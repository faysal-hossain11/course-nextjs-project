import React from 'react';
export const metadata = {
    title: "Insocour | Success Page",
    description: "Insocour | Success Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;