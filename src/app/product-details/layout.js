import React from 'react';
export const metadata = {
    title: "Insocour | Product Details Page",
    description: "Insocour | Product Details Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;