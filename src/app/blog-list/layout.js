import React from 'react';
export const metadata = {
    title: "Insocour | Blog-list Page",
    description: "Insocour | Blog-list Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;