import React from 'react';
export const metadata = {
    title: "Insocour | Blog Page",
    description: "Insocour | Blog Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;