import React from 'react';
export const metadata = {
    title: "Insocour | Shop Page",
    description: "Insocour | Shop Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;