import React from 'react';
export const metadata = {
    title: "Insocour | Home Page Two",
    description: "Insocour | Home Page Two Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;