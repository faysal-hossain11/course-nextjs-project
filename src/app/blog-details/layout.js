import React from 'react';
export const metadata = {
    title: "Insocour | Blog-details Page",
    description: "Insocour | Blog-details Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;