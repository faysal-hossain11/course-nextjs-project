import React from 'react';
export const metadata = {
    title: "Insocour | Sign In Page",
    description: "Insocour | Sign In Page Education Nextjs Template",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;